#include <stdio.h>
#include <add.h>
#include <sma.h>

int main() {
	/* Hello World
	printf("Hello world");
	return 0; */

	/* Assembly feat. C
	float n1 = 1.1;
	float n2 = 2.2;
	
	float c = add_c(n1, n2);
	float a = add_asm(n1, n2);
	
	printf("C subroutine sum = %f\n", c);
	printf("Assembly subroutine sum = %f\n", a);
	
	return 0; */
	
	/* Moving average filter */
	// TODO add some code to feed an array of values into the moving filter
}